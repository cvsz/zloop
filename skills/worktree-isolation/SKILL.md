# Worktree Isolation

Assign each mutating agent a unique branch/worktree, sync from a known base revision, define ownership when practical, detect overlapping diffs before integration, and merge only after verification.
