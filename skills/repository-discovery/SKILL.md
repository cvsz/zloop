# Repository Discovery

Build the minimum repository context needed for a correct plan. Inspect `AGENTS.md`, README, architecture/roadmap/planning docs first, then relevant source, tests, CI, dependency manifests, and failure evidence. Avoid whole-repository dumping when targeted search is possible.

Output a relevant file map, constraints, gates, failures, unknowns, and dependencies.
