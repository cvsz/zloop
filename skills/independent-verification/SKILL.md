# Independent Verification

Map each acceptance criterion to one or more objective checks. Prefer deterministic checks, run narrow checks before broad regressions, and return `PASS`, `FAIL`, or `INCONCLUSIVE` with evidence for every criterion.
